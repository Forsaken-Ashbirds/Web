/**
 * Supported locales within the application.
 *
 * @remarks
 * Extend this enum when adding additional languages to the project to ensure
 * type-safe locale handling across the codebase.
 */
export enum Locale {
	German = 'de',
	English = 'en'
}
